def fib(x)
    if x < 3 then
        1
    else
        fib(x-1) + fib(x-2)


def printstar(n x)
    for i = 1, i < n, 1 in
        x + 1


def binary| 5 (LHS RHS)
    if LHS then
        1
    else if RHS then
        1
    else
        0

def unary! (v)
    if v then
        0
    else
        1